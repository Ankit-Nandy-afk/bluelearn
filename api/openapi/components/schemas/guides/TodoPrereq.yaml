type: object
additionalProperties: false
description: A prerequisite guide that doesn't exist yet, noted while drafting.
properties:
  title:
    type: string
    minLength: 1
    maxLength: 200
    example: Introduction to functions
  summary:
    type: string
    maxLength: 500
    example: What a function is and how to read function notation.
required: [title, summary]
